# 다크웹 유출 검증

다크초코 4기 검증 절차다. 유출 주장 하나를 9단계로 처리한다.
Claude Code는 `skills/darkweb-verify-ko/SKILL.md` 를, Codex는 이 파일을 읽는다.
**둘 다 같은 `skills/darkweb-verify-ko/references/` 를 본다.**

## 절대 원칙 넷

| | 원칙 |
|---|---|
| 1 | **판정은 사람이 한다.** 8단계를 대신 내리지 않는다. 6단계에서 참고 판정까지만 낸다 |
| 2 | **원본 개인정보를 AI에 넣지 않는다.** 4단계에서 패턴으로 바꾼 뒤 쓴다. 4단계 입력만 원문이다 |
| 3 | **재료에 없는 정보를 채우지 않는다.** 모르면 못 봄으로 적는다 |
| 4 | **적신호 하나로 허위를 확정하지 않는다** |

## 상태값

빈칸으로 남기지 않는다. 넷 중 하나를 쓴다.

| 표기 | 뜻 |
|---|---|
| 확인함 | 봤고 값이 있다 |
| 없음 | 봤는데 없다 |
| 못 봄(이유) | 보려 했는데 막혔다. 이유에 수단과 질의를 적는다 |
| 안 봄 | 이번 범위가 아니었다 |

없음과 못 봄은 다르다. 못 본 것을 없음으로 적으면 나중에 실제로 빈 것으로 읽힌다.

## 서는 자리

이 절차는 4단계에서 6단계까지를 끊김 없이 이어서 돈다.

```
[사람]  1 발견 → 3 게시글 확인 (체크리스트, 파일 트리 확보)
             ↓  이 결과를 입력으로 넣는다
[AI]    2 사전 확인 (안 했으면) → 4 마스킹 → 5 재료 합치기 → 6 판정 근거 산출
             ↓  중간에 멈추지 않는다
[사람]  7 검토 → 8 판정
             ↓  필요하면 다시 부른다
[AI]    9 DB 입력
```

3단계는 호출 전에 사람이 준비하는 입력이다. 대신 수행하지 않는다.
7과 8단계는 산출이 끝난 뒤 사람이 한다.
9단계는 별도 호출이다. 조사가 중간에 끊긴 것이 아니다.

## 단계별 문서

필요한 단계 파일만 읽는다. 전부 읽지 않는다.

참고 문서는 아래 둘 중 **있는 쪽**에 있다. 실행 위치에 따라 다르다.

    skills/darkweb-verify-ko/references/          레포에서 실행할 때
    06_도구/skill/darkweb-verify-ko/references/   프로젝트 폴더에서 실행할 때

| 단계 | 담당 | 파일 이름 |
|---|---|---|
| 2 사전 확인 | AI | `stage2-precheck.md` |
| 3 게시글 확인 | 사람 | `stage3-post.md` |
| 4 마스킹 | AI | `stage4-masking.md` |
| 5 재료 합치기 | AI | `stage5-merge.md` |
| 6 판정 근거 | AI | `stage6-toolkit.md` |
| 7 검토 | 사람 | `stage7-review.md` |
| 8 판정 | 사람 | `stage8-verdict.md` |
| 9 DB 입력 | AI | `stage9-db.md` |
| 파일 트리 | AI | `tree-analysis.md` |
| 발행처 목록 | 참고 | `publishers.md` |

도구도 마찬가지다. `tools/` 또는 `06_도구/tools/` 에 있다.

## 판정 다섯 축

기준 전문은 `stage6-toolkit.md` 에 있다.

| 축 | 값 |
|---|---|
| 검증 분류 | A / B / C / D |
| 진위 판정 | 확인됨 / 신뢰성 높음 / 미확인 / 신뢰성 낮음 / 허위 |
| 신규성 판정 | 신규 유출 / 재유포 / 혼합 / 확인불가 |
| 판정 신뢰도 | 높음 / 중간 / 낮음 |
| 위험도 | 다섯 항목 각각. 총점을 내지 않는다 |

축을 합치지 않는다. 서로 어긋나도 된다.
위험도에 진위를 반영하지 않는다. 미확인이라고 위험도를 깎으면 실제 위험을 과소평가한다.

## 하면 안 되는 것

| | 왜 |
|---|---|
| 토큰이 살아있는지 확인하려고 요청을 보내는 것 | 남의 자격증명을 실제로 쓰는 행위다. 존재와 패턴까지만 |
| 유출 파일을 내려받는 것 | 다운로드 링크는 존재만 기록한다 |
| 유사 상호나 유사 도메인으로 갈아타는 것 | 도메인은 문자 단위로 대조한다 |
| 검색으로 못 찾은 것을 없음으로 적는 것 | 못 봄에 수단과 질의를 적는다 |
| 게시하는 것 | 초안까지만. 게시는 사람이 한다 |

## 정본

**정본은 팀 노션이다.** 이 레포와 노션이 다르면 노션이 맞다.
